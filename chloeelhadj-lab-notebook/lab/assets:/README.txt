Drop real files here, matching the filenames referenced in lab/harney.html:

  screenshot-workflow.png   — a real screenshot of the order sheet / dashboard
  prototype-demo.mp4        — a recording (or embed) of the actual prototype running
  field-notes.mp4 or .gif   — a short screen recording narrating the workflow

Once a file is here, open lab/harney.html and swap the matching "frame-empty" div
for the commented-out <img> or <video> tag directly below it in that block.

This folder is intentionally empty in the shipped version — no fake evidence.
